In this directory, are present a bunch of `.typ` files that contain random sources of    
bibliography that is used for developing the projects, a collection of notes per se.

Use the **typst** compiler to compile the notes into a pdf.

**CMD:** for compiling the source files as a single reference document use     
```typst compile src/notes.typ --root ..```
